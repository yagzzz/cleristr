import Image from "next/image";
import Link from "next/link";

export function AuthShell({ eyebrow, title, description, children, alternate }: { eyebrow: string; title: string; description: string; children: React.ReactNode; alternate: { label: string; href: string; action: string } }) {
  return <section className="site-container grid min-h-[calc(100svh-8rem)] items-center py-6 sm:py-10"><div className="auth-shell"><aside className="auth-aside"><Image src="/media/products/baggy-esofman.webp" alt="CLERIS Baggy ürün görseli" fill sizes="(max-width: 900px) 100vw, 48vw" className="object-cover" priority /><div className="auth-aside-overlay" /><div className="relative z-10 flex h-full flex-col justify-between p-7 text-white sm:p-10"><p className="eyebrow rounded-[10px] border border-white/20 bg-black/15 px-3 py-2 text-white/75 backdrop-blur w-fit">CLERIS ACCOUNT</p><div><h2 className="max-w-[8ch] text-5xl font-black leading-[.84] tracking-[-.07em]">DROP INTO<br /><span className="text-acid">YOUR SPACE.</span></h2><p className="mt-5 max-w-sm text-sm leading-6 text-white/70">Favoriler, sipariş adımları ve iletişim tercihlerin tek bir güvenli hesapta tutulur.</p></div></div></aside><div className="auth-panel"><p className="eyebrow">{eyebrow}</p><h1 className="mt-4 text-4xl font-black tracking-[-.05em] sm:text-5xl">{title}</h1><p className="mt-3 max-w-md text-sm leading-6 text-muted">{description}</p><div className="mt-8">{children}</div><div className="mt-8 border-t border-black/10 pt-6"><p className="text-sm text-muted">{alternate.label}</p><Link href={alternate.href} className="secondary-button mt-3 w-full">{alternate.action}</Link></div></div></div></section>;
}
